# Test Environment Configuration
# This file is used when running tests with Jest

# Test Database Connection
DATABASE_URL="postgresql://postgres:testpassword123@localhost:5433/pitlane_test?schema=public"
DIRECT_URL="postgresql://postgres:testpassword123@localhost:5433/pitlane_test?schema=public"

# Test Environment Variables
NODE_ENV=test
PORT=3001

# Auth0 Test Configuration (use test credentials or mock values)
AUTH0_DOMAIN=test-domain.auth0.com
AUTH0_AUDIENCE=test-audience
AUTH0_CLIENT_ID=test-client-id
AUTH0_CLIENT_SECRET=test-client-secret

# Other test-specific configurations
LOG_LEVEL=error